apiVersion: v1
kind: Service
metadata:
  name: eks-rag-service  # This name is fine
spec:
  selector:
    app: eks-rag  # Changed to match deployment labels
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
